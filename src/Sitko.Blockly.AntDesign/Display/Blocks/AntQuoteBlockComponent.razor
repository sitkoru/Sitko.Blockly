@typeparam TEntity
@inherits QuoteBlockComponent<TEntity, AntDesignBlocklyListOptions>
<blockquote class="blockquote">
    <div>
        @((MarkupString) Block.Text)
    </div>
    <footer class="author">
        @if (ListOptions.Storage is not null && Block.Picture != null)
        {
            <div class="picture" style="background-image: url(@ListOptions.Storage.PublicUri(Block.Picture))">
            </div>
        }
        <cite>
            @{
                var authorText = Block.Author;
                @if (ListOptions.Storage is null || Block.Picture is null)
                {
                    authorText = $"— {authorText}";
                }
            }
            @if (!string.IsNullOrEmpty(Block.Link))
            {
                if (!string.IsNullOrEmpty(Block.Author))
                {
                    <a href="@Block.Link">@authorText</a>
                }
                else
                {
                    <a href="@Block.Link">Source</a>
                }
            }
            else if (!string.IsNullOrEmpty(Block.Author))
            {

                @authorText
            }
        </cite>
    </footer>
</blockquote>

